<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdp.zwy.buildbody.module.system.dao.ImgRelationDao">

    <resultMap type="com.cdp.zwy.buildbody.module.system.entity.ImgRelation" id="ImgRelationMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="relationType" column="relation_type" jdbcType="INTEGER"/>
        <result property="relationId" column="relation_id" jdbcType="INTEGER"/>
        <result property="imgUrl" column="img_url" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into build_body.img_relation(relation_type, relation_id, img_url, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.relationType}, #{entity.relationId}, #{entity.imgUrl}, #{entity.createTime})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into build_body.img_relation(relation_type, relation_id, img_url, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.relationType}, #{entity.relationId}, #{entity.imgUrl}, #{entity.createTime})
        </foreach>
        on duplicate key update
relation_type = values(relation_type) , relation_id = values(relation_id) , img_url = values(img_url) , create_time = values(create_time)     </insert>

</mapper>

